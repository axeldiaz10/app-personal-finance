export type Currency = {
    id: number,
    name: string,
    abreviature: string
}

export type Category = {
    id: number,
    name: string
}

export type Client = {
    id: number,
    name: string,
    email: string,
    photo_url: string,
    date_of_birth: string
}